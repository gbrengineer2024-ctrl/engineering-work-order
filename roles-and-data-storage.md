# สิทธิ์ผู้ใช้และการจัดเก็บข้อมูล

เอกสารนี้อธิบายพฤติกรรมของระบบ Hotel Maintenance Web App ตามโค้ดที่ใช้งานจริง ณ วันที่ 15 สิงหาคม 2026 โดยบทบาทของโรงแรมเก็บใน `maintenance_users.roleCode` และใช้เป็นเกณฑ์หลักในการควบคุมหน้าจอและ API ของงานซ่อมบำรุง

> ผู้ใช้ใหม่ที่เข้าสู่ระบบด้วย LINE และยังไม่มีโปรไฟล์ จะกรอกเพียงชื่อกับแผนก จากนั้นระบบกำหนดบทบาทเริ่มต้นเป็น `REPORTER` ผู้ดูแลระบบจึงสามารถปรับบทบาทภายหลังได้จากเมนูผู้ใช้งาน

## สิทธิ์ตามบทบาท

| ความสามารถ | ADMIN | SUPERVISOR | TECHNICIAN | REPORTER |
|---|:---:|:---:|:---:|:---:|
| ดู Dashboard และรายละเอียดใบงาน | ✓ | ✓ | ✓ | ✓ |
| สร้างใบงานพร้อมรูปก่อนงาน | ✓ | ✓ | ✓ | ✓ |
| แก้ไขข้อมูลใบงานและแนบไฟล์ | ✓ | ✓ | ✓ | ✓ |
| มอบหมายช่าง | ✓ | ✓ | – | – |
| เปลี่ยนเป็นเสร็จงาน | ✓ | ✓ | ✓ | – |
| ปิดใบงาน | ✓ | ✓ | – | – |
| ต้องแนบรูปหลังงานก่อนเสร็จงาน | ✓ | ✓ | ✓ | – |
| ดูรายชื่อช่างและภาระงาน | ✓ | ✓ | ✓ | ✓ |
| เพิ่มหรือแก้ไขข้อมูลช่าง | ✓ | ✓ | – | – |
| ดูคลังอะไหล่และส่งคำขอเบิก | ✓ | ✓ | ✓ | ✓ |
| เพิ่ม/แก้ไข/ลบอะไหล่, อนุมัติ และจ่ายอะไหล่ | ✓ | ✓ | – | – |
| จัดการผู้ใช้และเปลี่ยนบทบาท | ✓ | – | – | – |
| ตั้งค่า LINE Messaging API และทดสอบการส่ง | ✓ | – | – | – |

สิทธิ์ทุกข้อในตารางถูกตรวจซ้ำที่ฝั่งเซิร์ฟเวอร์ ไม่ได้อาศัยการซ่อนปุ่มบนหน้าจอเพียงอย่างเดียว ดังนั้นผู้ใช้ที่ไม่มีสิทธิ์จะถูกปฏิเสธแม้พยายามเรียก API โดยตรง

## ปุ่มสร้างใบงาน

ปุ่ม **สร้างใบงาน** แบบลอยถูกกำหนดให้แสดงเฉพาะหลังจากตรวจพบ session ที่เข้าสู่ระบบแล้ว และเฉพาะเมื่อบทบาทโรงแรมเป็นหนึ่งใน `ADMIN`, `SUPERVISOR`, `TECHNICIAN` หรือ `REPORTER` เท่านั้น ผู้ที่ยังอยู่หน้า Login หรือยังไม่มีโปรไฟล์บทบาทจะไม่เห็นปุ่มนี้ เพื่อไม่ให้ปุ่มทับหน้าเข้าสู่ระบบหรือหน้าที่ไม่สามารถสร้างรายการได้

ตำแหน่งปุ่มอยู่มุมขวาบนของหน้า Dashboard/หน้าปฏิบัติงานภายหลังเข้าสู่ระบบ เพื่อให้เข้าถึงได้รวดเร็วทั้งบนเดสก์ท็อปและมือถือ โดยไม่แสดงใน flow การยืนยันตัวตน

## แหล่งจัดเก็บข้อมูล

| ประเภทข้อมูล | ที่จัดเก็บจริง | รายละเอียด |
|---|---|---|
| ใบงาน, สถานะ, ผู้แจ้ง, สถานที่, ประวัติ | ฐานข้อมูล TiDB/MySQL | จัดการผ่าน Drizzle ORM และ tRPC บนเซิร์ฟเวอร์ |
| ผู้ใช้, แผนก, บทบาท และความสัมพันธ์กับช่าง | ฐานข้อมูล TiDB/MySQL | ตาราง `maintenance_users`, `technicians` และ `users` |
| รายการอะไหล่และคำขอเบิก | ฐานข้อมูล TiDB/MySQL | ตาราง `parts` และ `part_issues` |
| รูปก่อน–หลังงานและไฟล์แนบ | S3 ของระบบ | ฐานข้อมูลเก็บเฉพาะ metadata/URL ไม่เก็บ bytes ของรูปในตาราง |
| การตั้งค่า LINE และบันทึกสถานะส่งล่าสุด | ฐานข้อมูล TiDB/MySQL | Token ถูกเก็บในฝั่งเซิร์ฟเวอร์และไม่ส่งคืนผ่าน API หน้าจอ |

Google Sheets ที่ใช้เป็นข้อมูลตั้งต้นเป็นแหล่งอ้างอิงสำหรับโครงสร้างและการนำเข้าข้อมูลเดิมเท่านั้น **เวอร์ชันปัจจุบันไม่ได้เขียนข้อมูลกลับไปยัง Google Sheets และไม่ได้ซิงก์อัตโนมัติ** ดังนั้นการสร้างหรือแก้ไขใบงานในเว็บจะบันทึกลงฐานข้อมูลของเว็บแอปโดยตรง หากต้องการซิงก์กลับ Google Sheets จะต้องเพิ่ม integration เฉพาะส่วนพร้อมกำหนดทิศทางการซิงก์และกติกาการแก้ข้อมูลชนกันก่อนใช้งานจริง

## เอกสารอ้างอิงภายในโครงการ

| ไฟล์ | ขอบเขตที่อ้างอิง |
|---|---|
| `server/routers.ts` | นโยบาย RBAC ของ API และการตั้งค่า LINE |
| `server/db.ts` | การอ่าน/เขียนข้อมูลธุรกิจและการแจ้งเตือน |
| `drizzle/schema.ts` | นิยามตารางฐานข้อมูล |
| `client/src/pages/ModernHome.tsx` | เงื่อนไขและตำแหน่งปุ่มสร้างใบงาน |
| `client/src/lib/workOrderCreateTrigger.ts` | เงื่อนไขความสามารถในการสร้างใบงาน |
