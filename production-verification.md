# Production Verification Notes

## 14 August 2026 — Parts Manager

URL tested: `https://hotelmaintai-e5vycneh.manus.space/?release=parts-manager-20260814`

- เข้าสู่ระบบด้วยบัญชี ADMIN และเปิดเมนู **อะไหล่** จาก sidebar สำเร็จ
- เมนูเปิด dialog **คลังอะไหล่และรายการเบิก** แทน placeholder เดิม
- ยืนยันการโหลดข้อมูลจริง: อะไหล่ 5 รายการ, คงเหลือรวม 154, ต่ำกว่าขั้นต่ำ 1
- ยืนยันว่ามีปุ่ม **เพิ่มอะไหล่**, **ขอเบิก**, **อนุมัติ**, และ **จ่าย** พร้อมรายการเบิกที่มีสถานะ REQUESTED, APPROVED และ ISSUED
- ไม่ได้สร้างหรือแก้ไขข้อมูล production ระหว่างการตรวจ

## 14 August 2026 — Parts Manager responsive follow-up

URL tested: `https://hotelmaintai-e5vycneh.manus.space/?release=parts-width-20260814`

- ยืนยันว่า dialog คลังอะไหล่รุ่นล่าสุดใช้พื้นที่ desktop ได้กว้างขึ้นและไม่มีแถบเลื่อนแนวนอนในเนื้อหาที่แสดง
- ตารางคลังแสดงรหัส รายการ ที่เก็บ สต็อกพร้อมใช้ และต้นทุนครบถ้วน
- รายการเบิกล่าสุดยังแสดงสถานะและ action ที่ตรงกับสิทธิ์ ADMIN
- ไม่ได้สร้าง แก้ไข อนุมัติ หรือจ่ายข้อมูล production ระหว่างการตรวจ

## 14 August 2026 — Work-order detail verification

URL tested: `https://hotelmaintai-e5vycneh.manus.space/?release=parts-width-20260814`

- เปิดรายละเอียด `WO-20260814-7802` ที่มีสถานะกำลังดำเนินการได้จากตารางใบงานล่าสุด
- Dialog แสดงพื้นที่ รายละเอียด ส่วนรูปก่อนเริ่มงาน และส่วนรูปหลังเสร็จงานอย่างชัดเจน
- ในส่วนรูปหลังงานมีข้อความระบุว่าเป็นเงื่อนไขจำเป็นก่อนเปลี่ยนสถานะเป็นเสร็จงาน และเมื่อยังไม่มีรูป ระบบยังแสดงปุ่มทำเครื่องหมายว่าเสร็จตาม workflow แต่ API มี regression guard ปฏิเสธการปิดงานโดยไม่มีรูป AFTER
- ตรวจแบบอ่านอย่างเดียว: ไม่ได้แนบรูป เปลี่ยนผู้รับผิดชอบ หรือเปลี่ยนสถานะใบงาน production

## 14 August 2026 — Recovery state and dashboard

URL checked: `https://hotelmaintai-e5vycneh.manus.space/?release=after-photo-guard-20260814`

- หลังผ่านช่วงตรวจ session ระบบแสดง dashboard ของบัญชี ADMIN ได้ตามปกติ ไม่ค้างที่หน้ากำลังตรวจสอบ
- หน้าหลักมี navigation ภาษาไทย, สรุปใบงาน, ปุ่มสร้างใบงานด้านบน และรายการใบงานล่าสุด โดยไม่พบข้อผิดพลาด layout ใน viewport เดสก์ท็อปที่ตรวจ

## 14 August 2026 — Completion guard verification

URL checked: `https://hotelmaintai-e5vycneh.manus.space/?release=after-photo-guard-20260814`

- เปิดรายละเอียด `WO-20260814-7802` ขณะที่ส่วนรูปหลังเสร็จงานยังไม่มีรูป AFTER
- ปุ่ม **ทำเครื่องหมายว่าเสร็จ** แสดงสถานะปิดใช้งานและไม่ปรากฏเป็น control ที่กดได้ จึงไม่ส่งคำขอเปลี่ยนสถานะ
- ไม่มีการเปลี่ยนสถานะ แนบรูป หรือแก้ไขข้อมูล production ระหว่างการตรวจ

## 14 August 2026 — Requester profile in work-order form

URL checked: `https://hotelmaintai-e5vycneh.manus.space/`

- เปิดฟอร์มสร้างใบงานของบัญชี ADMIN ที่เข้าสู่ระบบด้วย LINE แล้ว
- ฟอร์มแสดงผู้แจ้ง `BOM` และแผนก `ENGINEERING` จาก maintenance profile ใน session โดยอัตโนมัติ
- ฟิลด์สร้างใบงานยังเป็น `Room / Location` พร้อมตัวเลือกหมวดหมู่และความสำคัญที่ครบถ้วน
- ปิดฟอร์มโดยไม่กรอกหรือบันทึกข้อมูล production

### ขอบเขตการยืนยัน flow ลงทะเบียนครั้งแรก

| หัวข้อ | หลักฐานที่ยืนยันแล้ว | สถานะ |
| --- | --- | --- |
| ตรวจข้อมูลชื่อและแผนก | Regression test ปฏิเสธข้อมูลที่ไม่ครบก่อนเรียกฐานข้อมูล | ผ่าน |
| ผูกโปรไฟล์กับ LINE session | Regression test ยืนยันว่าใช้ `ctx.user.openId` ไม่รับ user ID จาก client | ผ่าน |
| ผู้แจ้งในใบงาน | Regression test และ production form ยืนยันว่าผู้แจ้งใช้ session/profile ไม่รับค่า forged จาก client | ผ่าน |
| หน้าจอผู้ใช้ LINE รายใหม่ | โค้ดมีฟอร์มชื่อและ dropdown แผนก และบันทึกเป็น REPORTER ตาม server workflow แต่รอบนี้ไม่ logout ADMIN หรือสร้างบัญชีจริงเพื่อหลีกเลี่ยงการเปลี่ยน session/ข้อมูล production | รอทดสอบด้วยบัญชี LINE ใหม่ |

### Admin Settings — ตรวจบน production

ตรวจด้วยบัญชี ADMIN (BOM) เมื่อ 14 ส.ค. 2026 โดยไม่กรอกหรือบันทึกข้อมูล พบว่าหน้า **ตั้งค่า** โหลดได้ตามปกติ มีช่อง `Channel Access Token` แบบ password, ช่อง `User ID หรือ Group ID`, สวิตช์เปิดแจ้งเตือน/งานเร่งด่วน/เกิน SLA, ปุ่มส่งข้อความทดสอบ และปุ่มบันทึกการตั้งค่า สถานะการเชื่อมต่อปัจจุบันแสดงว่า **รอข้อมูลเชื่อมต่อ** และยังไม่มีประวัติการส่ง ซึ่งสอดคล้องกับการไม่มี credentials จริงในรอบตรวจนี้

### การทดสอบแจ้งเตือน LINE บน production

- วันที่ตรวจ: 14 สิงหาคม 2026
- สร้างใบงานทดสอบที่ผู้ใช้อนุมัติ: `WO-20260814-5215` — Room / Location: `TEST-LINE-20260814`
- รายละเอียด: ทดสอบการแจ้งเตือน LINE เมื่อมอบหมายงานให้ช่าง และแจ้งผู้แจ้งเมื่องานเสร็จ
- แนบรูปก่อนงานแล้ว และเปิดรายละเอียดเพื่อมอบหมายช่างเรียบร้อย
- หลังมอบหมาย: รายการใบงานแสดงผู้รับผิดชอบเป็น `Bom` และสถานะ `มอบหมายแล้ว`
- แนบรูปหลังงานสำเร็จ และเปลี่ยนใบงานเป็นสถานะ `เสร็จงาน` สำเร็จ
- การดำเนินการดังกล่าวเรียก notification handler ของเหตุการณ์มอบหมายและงานเสร็จตาม workflow ที่เผยแพร่แล้ว
- ใช้ใบงานทดสอบที่ระบุชัดเจนเท่านั้น โดยไม่แก้ไขใบงานใช้งานจริง

### ผลทดสอบ LINE Messaging API ใน Admin Settings

- หน้า Admin Settings แสดง Channel Access Token และ User ID ในรูปแบบปกปิดค่า
- สถานะการเชื่อมต่อเปลี่ยนเป็น **พร้อมใช้งาน** และรายการส่งล่าสุดเป็น `SENT`
- หน้าจอแสดงข้อความ **ส่งข้อความทดสอบแล้ว** ยืนยันว่าการส่งข้อความทดสอบด้วย User ID เดียวกับบัญชีที่ล็อกอินสำเร็จ

### ขอบเขตการยืนยันข้อความตามเหตุการณ์

- หน้าจอ production ยืนยันการส่งข้อความทดสอบไปยัง User ID ของบัญชีที่ล็อกอินได้สำเร็จ และใบงาน `WO-20260814-5215` ผ่านเหตุการณ์ `ASSIGNED` และ `COMPLETED` โดยไม่มีข้อผิดพลาดหน้าเว็บหรือการเปลี่ยนสถานะค้าง
- การยืนยันการเห็นข้อความของแต่ละเหตุการณ์ในห้องสนทนา LINE ของผู้รับยังต้องให้ผู้ใช้ตรวจจากแอป LINE โดยตรง เนื่องจาก session ของเว็บไม่สามารถอ่านกล่องข้อความ LINE ได้

### การทดสอบซ้ำการแจ้งเตือนเมื่อมอบหมายงาน

- วันที่ตรวจ: 15 สิงหาคม 2026
- ผู้ใช้ยืนยันให้ส่งการแจ้งเตือนการมอบหมายซ้ำ 1 ครั้งสำหรับใบงานทดสอบ `WO-20260814-5215` ไปยังช่าง `Bom`
- เปิดรายละเอียดใบงานบน production พบว่าเลือกช่าง `Bom` อยู่แล้ว และกด **บันทึกการมอบหมาย** สำเร็จ
- รีเฟรช production แล้วพบว่าใบงานเปลี่ยนจาก `เสร็จงาน` เป็น `มอบหมายแล้ว` และยอดงานกำลังดำเนินการใน dashboard เพิ่มจาก 6 เป็น 7 จึงยืนยันได้ว่าธุรกรรมมอบหมายสำเร็จ
- หน้าเว็บไม่แสดงข้อผิดพลาด และ console ของ browser ไม่มี error หลังส่งคำขอ
- ตรวจหน้า Admin Settings แบบอ่านอย่างเดียวหลังทดสอบ พบการตั้งค่า LINE อยู่ในสถานะ `พร้อมใช้งาน` และ `ส่งล่าสุด: SENT` โดยไม่มีสถานะ failure ปรากฏ ทั้งนี้ค่านี้เป็นสถานะการส่งล่าสุดระดับ integration จึงไม่สามารถใช้ยืนยันการส่งถึงช่างคนใดคนหนึ่งหรือยืนยันการแสดงข้อความในแอป LINE ได้
- การยืนยันการได้รับข้อความในห้องสนทนา LINE ของช่างยังต้องตรวจจากแอป LINE โดยตรง เนื่องจากเว็บไม่สามารถอ่าน inbox ของ LINE ได้

### ผลวิเคราะห์หลังผู้รับยืนยันว่าไม่ได้รับข้อความ

- ผู้ใช้ยืนยันหลังการทดสอบซ้ำว่า ช่าง `Bom` **ไม่ได้รับ** ข้อความ `[มอบหมายงาน]` ของ `WO-20260814-5215`
- ตรวจข้อมูลที่ใช้ส่งแบบปกปิดแล้วพบว่าระเบียนช่าง Bom ไม่มี `technicians.lineUserId` และ maintenance profile ที่เชื่อมกับช่างก็ไม่มี `maintenance_users.lineUserId` เช่นกัน
- บัญชีที่เชื่อมกับระเบียนช่างดังกล่าวเป็นบัญชีจาก Google ไม่ใช่ LINE Login จึงไม่มี LINE user ID ที่ระบบจะใช้เป็นผู้รับปลายทางได้
- สาเหตุที่การแจ้งงานเสร็จทำงานได้ แต่การมอบหมายไม่ถึงช่าง คือใบงานทดสอบมี LINE user ID ของผู้แจ้ง ขณะที่ช่าง Bom ไม่มี LINE user ID ที่เชื่อมกับข้อมูลช่าง

### การป้องกันสถานะส่งสำเร็จลวง

- ปรับระบบให้เมื่อเหตุการณ์ `ASSIGNED` ไม่มี LINE user ID ของช่าง ระบบจะบันทึกสถานะ integration เป็น `SKIPPED` พร้อมเหตุผลที่ไม่เปิดเผย ID แทนการคงค่า `SENT` จากการส่งครั้งก่อน
- API รายชื่อช่างจะคืนเพียงสถานะ `isLineConnected` เพื่อใช้แสดงความพร้อมของการแจ้งเตือน โดยไม่คืน LINE user ID ของช่างให้หน้าเว็บสาธารณะ
- ก่อนทดสอบการส่งมอบหมายซ้ำ ต้องให้ Bom เข้าสู่ระบบด้วย LINE และให้ ADMIN กำหนดบทบาทบัญชีที่ลงทะเบียนเป็น `TECHNICIAN` เพื่อสร้าง/ซิงก์ระเบียนช่างที่มี LINE user ID โดยอัตโนมัติ

### Fallback การแจ้งมอบหมายไปยัง User/Group ID

- เมื่อมอบหมายงานให้ช่างที่ยังไม่มี LINE user ID ระบบจะส่งข้อความไปยัง User/Group ID จาก Admin Settings แทน เพื่อให้ผู้ดูแลหรือกลุ่มช่างไม่พลาดเหตุการณ์
- ผลลัพธ์นี้จะถูกบันทึกด้วยสถานะ `SENT_FALLBACK` เพื่อแยกจาก `SENT` ซึ่งส่งตรงถึง LINE ของช่าง และไม่กระทบการแจ้งงานเสร็จที่ยังส่งเฉพาะผู้แจ้งตาม LINE user ID ของผู้แจ้ง

### ผลทดสอบ production — 14 สิงหาคม 2026 เวลา 22:04 ICT

- มอบหมาย `WO-20260814-5215` ให้ Bom ซ้ำหลังเผยแพร่ fallback สำเร็จ สถานะใบงานเปลี่ยนเป็น `ASSIGNED` และหน้า production แสดงสถานะ **มอบหมายแล้ว**
- แถวการตั้งค่า LINE ยืนยัน `isEnabled = true`, `lastDeliveryStatus = SENT_FALLBACK` และ `lastError = null` จึงยืนยันว่าระบบได้รับการตอบรับจาก LINE Messaging API สำหรับ User/Group ID สำรองแล้ว
- ยังต้องยืนยันการปรากฏของข้อความใน LINE ของ User/Group ID ผู้รับจริง เนื่องจากการตอบรับจาก API ไม่ยืนยันการอ่านข้อความปลายทาง

### ผลทดสอบ production — Deep link ของใบงาน

- เปิด URL `https://hotelmaintai-e5vycneh.manus.space/?woId=WO-20260814-5215` ใน session ADMIN ที่เข้าสู่ระบบแล้ว
- ระบบโหลด dashboard และเปิด dialog รายละเอียดของ `WO-20260814-5215` อัตโนมัติ โดยแสดงสถานะ **มอบหมายแล้ว**, รูปก่อน–หลังงาน และปุ่มจัดการใบงานครบถ้วน
- การทดสอบยืนยันว่าผู้รับสามารถกดลิงก์จากข้อความ LINE เพื่อไปยังใบงานที่เกี่ยวข้องได้โดยตรง หลังเข้าสู่ระบบแล้ว

### ผลทดสอบ end-to-end ที่ผู้ใช้ยืนยัน

- ส่งข้อความ `[มอบหมายงาน]` เพิ่มเติมหลังเผยแพร่ deep link แล้ว โดย LINE Messaging API ตอบรับด้วย `SENT_FALLBACK` และไม่มี error
- ผู้ใช้ยืนยันว่าได้รับข้อความล่าสุดครบถ้วน และกดลิงก์ในข้อความเพื่อเปิดรายละเอียด `WO-20260814-5215` ได้สำเร็จ
- การแจ้งมอบหมายจึงครอบคลุมทั้งผู้รับตรงที่เชื่อม LINE และ User/Group ID สำรองใน Admin Settings; กรณีช่างยังไม่ได้เชื่อม LINE จะใช้เส้นทางสำรองดังกล่าว

### ผลตรวจฟอร์มสร้างใบงาน — 15 สิงหาคม 2026

- เปิดฟอร์ม **สร้างใบงาน** บน production ด้วยบัญชี ADMIN โดยไม่กรอกหรือบันทึกข้อมูล พบข้อมูลผู้แจ้งและแผนกจาก session, ฟิลด์ `Room / Location`, หมวดหมู่, ความสำคัญ, ช่องอธิบาย และพื้นที่แนบรูปก่อนงานครบถ้วน
- ตรวจ viewport กว้าง 390px แล้วปุ่ม **สร้างใบงาน** หลักอยู่ในพื้นที่เนื้อหา ไม่มีปุ่มลอยทับหน้าจอหรือการล้นแนวนอนที่สังเกตได้
- การตรวจนี้ไม่ครอบคลุมการบันทึกใบงานหรือแนบไฟล์จากอุปกรณ์มือถือจริง จึงยังควรทดสอบขั้นตอนดังกล่าวก่อนเปิดใช้งานกับผู้ใช้จำนวนมาก
