# Verification Notes

## 14 สิงหาคม 2026

ตรวจหน้าหลักหลังลบ re-export เก่าจาก `Home.tsx` ที่ทำให้ Vite รายงานการอ้างอิง `./ModernHome` ผิดพลาดแล้ว ผลการตรวจพบว่า production build สำเร็จ และ Vitest ผ่านครบ 33 tests

| มุมมอง | ผลการตรวจ |
| --- | --- |
| Desktop 1440 × 900 | Dashboard แสดงผลปกติ ไม่ปรากฏหน้า error และตารางใบงานอยู่ในพื้นที่เนื้อหาที่อ่านได้ |
| Mobile 390 × 844 | Header, การ์ดสถิติ และปุ่ม `สร้างใบงาน` ด้านบนขวาแสดงครบ ไม่มีข้อความหรือปุ่มล้นขอบจอใน viewport ที่ตรวจ |

> ยังต้องทดสอบแบบมีผู้ใช้งานจริงสำหรับ LINE Login, การแนบรูปก่อนงาน, การปรับบทบาท ADMIN → TECHNICIAN และการบังคับรูปหลังงานก่อนจบงาน
