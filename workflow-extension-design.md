# แบบออกแบบการขยายระบบงานซ่อมบำรุง

เอกสารนี้กำหนดขอบเขตการพัฒนาจากคำขอเพิ่มการเบิกอะไหล่ สถานะรออะไหล่ รูปภาพหลายรูป สถานะช่าง เลขใบงานอัตโนมัติ และการจัดเก็บเอกสารใน Google Workspace โดยยึดหลักว่า **ไม่ทำให้ใบงานเดิม ข้อมูลสต็อก หรือรูปเดิมสูญหาย**

## 1. อะไหล่และสถานะ Pending Parts

| เรื่อง | พฤติกรรมที่จะใช้ |
|---|---|
| ปุ่มในใบงาน | ช่างที่ได้รับมอบหมายและผู้ดูแลเปิดคำขอเบิกจากรายละเอียดใบงานได้ |
| การอ้างอิง | คำขอเบิกเก็บ `woId` ที่ระบบออกอัตโนมัติอยู่แล้ว จึงไม่ต้องให้ผู้ใช้พิมพ์เลขใบงาน |
| ตัดสต็อก | เมื่อผู้มีสิทธิ์จ่ายอะไหล่ ระบบตัด `currentStockQty` และบันทึก `ISSUED`; ขั้นอนุมัติจะจอง `reservedQty` ก่อน |
| Pending Parts | เพิ่ม `PENDING_PARTS` เพื่อบอกว่างานหยุดรอของ/สั่งซื้อ และกลับเป็น `IN_PROGRESS` เมื่ออะไหล่พร้อม |
| Dashboard | แทนตัวชี้วัด “เกิน SLA” ด้วย “Pending Parts” เพื่อแสดงจำนวนใบงานที่รออะไหล่ |

> การตั้งเป็น `PENDING_PARTS` ไม่ตัดสต็อกเอง เพราะคำขออาจเป็นการรอสั่งของที่ยังไม่มีในคลัง การตัดสต็อกเกิดเฉพาะหลังจ่ายอะไหล่จริงเท่านั้น

## 2. รูปภาพในใบงาน

ระบบจะคงประเภทภาพ `BEFORE`, `AFTER` และ `OTHER` เดิม แต่ยอมรับหลายรายการต่อประเภท โดยเลือกไฟล์หลายไฟล์ได้ในครั้งเดียว ฟอร์มจะแสดงรายการไฟล์ที่เลือกก่อนอัปโหลด และในรายละเอียดใบงานแต่ละ thumbnail จะกดเปิดตัวดูภาพแบบเต็มหน้าจอได้

การบังคับมีภาพหลังงานก่อนเปลี่ยนเป็น `COMPLETED` ยังคงใช้เงื่อนไขเดิม และตรวจว่ามีอย่างน้อยหนึ่งภาพประเภท `AFTER`

## 3. สถานะช่างตามหน้างาน

| สถานะ | ความหมาย | ผู้เปลี่ยนได้ |
|---|---|---|
| `AVAILABLE` / พร้อมรับงาน | อยู่ในเวรและพร้อมรับงานใหม่ | ช่างเอง, SUPERVISOR, ADMIN |
| `ON_SHIFT` / เข้าเวร | บันทึกเวลาเริ่มเวร แต่ยังไม่ยืนยันความพร้อมรับงาน | ช่างเอง, SUPERVISOR, ADMIN |
| `OFF_SHIFT` / ออกเวร | สิ้นสุดเวร ไม่ควรเป็นตัวเลือกเริ่มต้นสำหรับการมอบหมาย | ช่างเอง, SUPERVISOR, ADMIN |
| `LEAVE` / หยุด | ไม่พร้อมรับงานเนื่องจากวันหยุด/ลา | SUPERVISOR, ADMIN |

เพื่อให้สถานะสะท้อนหน้างานจริง จะเก็บประวัติการเปลี่ยนสถานะพร้อมเวลาและผู้ดำเนินการ ไม่ใช้เพียงค่าสถานะล่าสุด ช่างเช็กอิน/เช็กเอาต์ได้เอง ส่วนหัวหน้างานและผู้ดูแลแก้ไขได้เมื่อช่างลืมอัปเดต

## 4. เลขใบงาน

เลข `woId` เป็นรหัสที่ backend สร้างอัตโนมัติเท่านั้น รูปแบบปัจจุบัน `WO-YYYYMMDD-XXXX` จะคงไว้ และไม่เปิดเป็นช่องกรอกบนฟอร์มสร้างงาน ทุกคำขอเบิกและรูปแนบจะผูกกับรหัสนี้จากใบงานที่เปิดอยู่โดยตรง

## 5. Google Drive และ Google Sheets

สำหรับรูปใหม่ ระบบที่เผยแพร่ต้องเรียก Google Drive API จาก backend ด้วยสิทธิ์ที่ปลอดภัย ไม่สามารถพึ่งคำสั่งบนเครื่องพัฒนาในขณะผู้ใช้กำลังอัปโหลดได้ ดังนั้นจะดำเนินการเป็นสองชั้น:

1. สร้างโฟลเดอร์กลางใน Google Drive และเพิ่มแท็บ `12_Role_Permissions` ใน Google Sheets ที่ระบุ ตามสิทธิ์ Google Workspace ที่ผู้ใช้อนุมัติ
2. คง S3 เป็นพื้นที่เก็บรูปเดิมและ fallback ที่ใช้งานอยู่ ก่อนเพิ่ม Google Drive runtime integration ต้องเตรียม Google Cloud OAuth/Service Account credential แบบ server-side และทดสอบการเขียนไฟล์จริง โดยไม่บันทึก credential ลง source code

แนวทางนี้ป้องกันรูปใบงานสูญหายและช่วยให้เปลี่ยนผู้ให้บริการจัดเก็บไฟล์ได้โดยไม่ทำลาย reference เดิม

## 6. ตารางสิทธิ์โดยสรุป

| ความสามารถ | REPORTER | TECHNICIAN | SUPERVISOR | ADMIN |
|---|---:|---:|---:|---:|
| สร้างและติดตามใบงานของตน | ใช่ | ใช่ | ใช่ | ใช่ |
| รับ/อัปเดตงานที่ได้รับมอบหมาย | ไม่ | ใช่ | ใช่ | ใช่ |
| ขอเบิกอะไหล่จากใบงาน | ไม่ | ใช่ | ใช่ | ใช่ |
| อนุมัติ/จ่ายและตัดสต็อก | ไม่ | ไม่ | ใช่ | ใช่ |
| ตั้ง Pending Parts และแก้สถานะช่าง | ไม่ | เฉพาะสถานะตน | ใช่ | ใช่ |
| จัดการผู้ใช้ การตั้งค่า LINE และคลัง | ไม่ | ไม่ | บางส่วนตาม workflow | ใช่ |

รายละเอียดสิทธิ์ฉบับผู้ใช้งานจะคงไว้ใน `roles-and-data-storage.md` และจะเพิ่มลง Google Sheets ในแท็บที่ขอเมื่อสร้างสำเร็จ
